import genesis from "./ot/genesis.json";

import obadiah from "./ot/obadiah.json";

// Add all the books you need in this object
const bibleBooks: Record<string, any> = {
  genesis,
  
  obadiah,
};

export default bibleBooks;
